describe('Example Test', () => {
  it('should assert true is true', () => {
    expect(true).toBe(true);
  });
  it('should assert false is not true', () => {
    expect(false).not.toBe(true);
  });
});